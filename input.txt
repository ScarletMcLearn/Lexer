int main()
{
    freopen("input.txt","r",stdin);
    freopen("output.txt","w",stdout);


    string buffer;

    while(!cin.eof())
    {
        getline(cin, buffer);
        if(!buffer.empty())
        {
            dfa.detect(buffer);
            dfa.addEndLine();
        }
    }

    dfa.printTokens();

}
